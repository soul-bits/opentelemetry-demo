# Python dependencies for the scenarios/ package (controller + validator + PBT harness).
# Runtime uses only stdlib (json, os, tempfile, subprocess, threading, time) beyond these six pins.
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0
hypothesis>=6.100,<7.0
pytest>=8.0,<9.0
numpy>=1.26,<3.0
filelock>=3.13,<4.0
