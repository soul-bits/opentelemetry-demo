route:
  receiver: 'file-logger'
  group_by: ['alertname', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 5m

receivers:
  - name: 'file-logger'
    webhook_configs:
      - url: 'http://alert-webhook-server:5000/webhook'
        send_resolved: true
